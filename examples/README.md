# Examples

| Script / Notebook | Description |
|---|---|
| `run_training.ps1` | End-to-end: extract → train → rollout → animate for a single case |
| `predict_new_case.ps1` | Run inference on a new OpenFOAM case using a pre-trained model |

